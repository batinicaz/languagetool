name: Release
on:
  push:
    branches: ["main"]
permissions:
  contents: write
jobs:
  release:
    uses: batinicaz/gha/.github/workflows/release.yml@26cc6de635db027946310b51915b0106a56d2635 # ratchet:batinicaz/gha/.github/workflows/release.yml@v1.5.2
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
