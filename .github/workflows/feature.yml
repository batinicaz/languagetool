name: Build
on:
  push:
    branches: ["**", "!main"]
permissions:
  contents: read
  packages: write
  security-events: write
jobs:
  build:
    uses: batinicaz/gha/.github/workflows/docker.yml@4eaeea879113acebe86a05453d88e58ba6d44713 # ratchet:batinicaz/gha/.github/workflows/docker.yml@v1.5.3
    with:
      runner: ubuntu-24.04-arm
      push: false
    secrets:
      GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
