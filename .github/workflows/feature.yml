name: Build
on:
  push:
    branches: ["**", "!main"]
permissions:
  contents: read
  packages: write
  security-events: write
jobs:
  build:
    uses: batinicaz/gha/.github/workflows/docker.yml@26cc6de635db027946310b51915b0106a56d2635 # ratchet:batinicaz/gha/.github/workflows/docker.yml@v1.5.2
    with:
      runner: ubuntu-24.04-arm
      push: false
    secrets:
      GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
