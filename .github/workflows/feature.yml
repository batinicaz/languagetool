name: Build
on:
  push:
    branches: ["**", "!main"]
permissions:
  contents: read
  packages: write
  security-events: write
jobs:
  build:
    uses: batinicaz/gha/.github/workflows/docker.yml@b22f66ab4ac3db4b4053ecdea3e57ef0da877839 # ratchet:batinicaz/gha/.github/workflows/docker.yml@v1.5.3
    with:
      runner: ubuntu-24.04-arm
      push: false
    secrets:
      GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
